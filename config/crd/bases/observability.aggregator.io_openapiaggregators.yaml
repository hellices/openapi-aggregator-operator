---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: openapiaggregators.observability.aggregator.io
spec:
  group: observability.aggregator.io
  names:
    kind: OpenAPIAggregator
    listKind: OpenAPIAggregatorList
    plural: openapiaggregators
    singular: openapiaggregator
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenAPIAggregator is the Schema for the openapiaggregators API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenAPIAggregatorSpec defines the desired state of OpenAPIAggregator
            properties:
              defaultPath:
                default: /v3/api-docs
                description: 'OpenAPI 경로 (예: /v3/api-docs)'
                type: string
              defaultPort:
                default: "8080"
                description: '포트 이름 또는 번호 (예: "http" 또는 "8080")'
                type: string
              displayNamePrefix:
                description: Swagger UI에 표시할 이름 prefix
                type: string
              ignoreAnnotations:
                default: false
                description: annotation 무시 여부 (true면 기본값만 사용)
                type: boolean
              labelSelector:
                additionalProperties:
                  type: string
                description: Deployment에 붙은 라벨 셀렉터
                type: object
              namespaceSelector:
                description: |-
                  Foo is an example field of OpenAPIAggregator. Edit openapiaggregator_types.go to remove/update
                  Foo string `json:"foo,omitempty"`
                  감시할 네임스페이스 (빈 값이면 전체)
                type: string
              pathAnnotation:
                default: openapi.aggregator.io/path
                description: OpenAPI 경로를 지정하는 annotation 키
                type: string
              portAnnotation:
                default: openapi.aggregator.io/port
                description: OpenAPI 포트를 지정하는 annotation 키
                type: string
            type: object
          status:
            description: OpenAPIAggregatorStatus defines the observed state of OpenAPIAggregator
            properties:
              collectedAPIs:
                description: CollectedAPIs contains information about the OpenAPI
                  specs that have been collected
                items:
                  description: APIInfo contains information about a collected OpenAPI
                    spec
                  properties:
                    annotations:
                      additionalProperties:
                        type: string
                      description: Annotations stores relevant annotations from the
                        resource
                      type: object
                    error:
                      description: Error is set if there was an error collecting the
                        spec
                      type: string
                    lastUpdated:
                      description: LastUpdated is when the spec was last successfully
                        collected
                      type: string
                    name:
                      description: Name is the name of the API (usually from displayNamePrefix
                        + deployment name)
                      type: string
                    namespace:
                      description: Namespace is the namespace of the kubernetes resource
                      type: string
                    resourceName:
                      description: ResourceName is the name of the kubernetes resource
                      type: string
                    resourceType:
                      description: ResourceType is the type of the kubernetes resource
                        (Deployment, StatefulSet, etc)
                      type: string
                    url:
                      description: URL is the full URL where the OpenAPI spec can
                        be accessed
                      type: string
                  required:
                  - lastUpdated
                  - name
                  - namespace
                  - resourceName
                  - resourceType
                  - url
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
